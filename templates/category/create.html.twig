{% extends 'base.html.twig' %}

{% block title %}Ajouter une nouvelle catégorie{% endblock %}

{% block body %}
    <h1>Créer une nouvelle ctégorie</h1>

    <div class="form mb-2">
        {{ include('category/_form.html.twig', {'button_label': 'Ajouter'}) }}
    </div>

    <a href="{{ path('categories_list') }}" class="btn btn-primary">Retour à la liste</a>
{% endblock %}