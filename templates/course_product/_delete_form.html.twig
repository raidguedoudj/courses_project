<form method="post" action="{{ path('app_course_product_delete', {'id': course_product.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ course_product.id) }}">
    <button class="btn">Delete</button>
</form>
