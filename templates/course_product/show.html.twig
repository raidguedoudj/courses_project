{% extends 'base.html.twig' %}

{% block title %}CourseProduct{% endblock %}

{% block body %}
    <h1>CourseProduct</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ course_product.id }}</td>
            </tr>
            <tr>
                <th>Quantity</th>
                <td>{{ course_product.quantity }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_course_product_index') }}">back to list</a>

    <a href="{{ path('app_course_product_edit', {'id': course_product.id}) }}">edit</a>

    {{ include('course_product/_delete_form.html.twig') }}
{% endblock %}
